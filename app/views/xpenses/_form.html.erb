<%= form_for(xpense) do |f| %>
  <% if xpense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(xpense.errors.count, "error") %> prohibited this xpense from being saved:</h2>

      <ul>
      <% xpense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
